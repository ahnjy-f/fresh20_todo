<!DOCTYPE html>
<html lang="ja">
    <head>
        <meta charset="UTF-8">
        
        <link rel="stylesheet"
        href="https:stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.css">
    </head>
    <body class="container">
        <%- include('nav') %>
        <p class="text-right">
            <%= typeof login !== "undefined" ? '"' + login.name + '" logined.' :'(not logined)' %>
        </p> 
        
