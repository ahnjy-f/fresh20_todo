<%-include('./header') %>

<h1 class="mt-3 mb-3"><%= __n('Top.Title') %></h1>

<p class="h5 mt-4"><%= __n('Top.Nearly Todo') %></p>
<ol class="list-group">
    <% for(let i in data){ %>
        <li class="list-group-item list-group-item-action">
          <a class="text-dark" href="/view?id=<%=data[i].id %>">
            <%=data[i]['datetime("finished","+9 hours")'] + ' ' + data[i].title %></a>
        </li>
    
    <%}%>
</ol>

<p class="h5 mt-4"><%= __n('Top.Passed Todo') %></p>
<ol class="list-group">
    <% for(let i in data2){ %>
        <li class="list-group-item list-group-item-action">
          <a class="text-dark" href="/view?id=<%=data2[i].id %>">
            <%=data2[i]['datetime("finished","+9 hours")'] + ' ' + data2[i].title %></a>

        </li>
    
    <%}%>
    <p class="h5 mt-4"><a href="/switch">Completed</a></p>
    <%if(count){%>
    <% for(let i in data3){ %>
        <li class="list-group-item list-group-item-action">
            <%=data3[i]["datetime(finished,'+9 hours')"] + ' ' + data3[i].title %>
        </li>
    
    <%}%>
    <%}%>
</ol>


<%-include('./footer')%>